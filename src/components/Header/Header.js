import React from "react";
import "./header.css";
import Logo from "../Logo/Logo";
const Header = () => {
  return (
    <div className="header">
      <Logo />
    </div>
  );
};

export default Header;
